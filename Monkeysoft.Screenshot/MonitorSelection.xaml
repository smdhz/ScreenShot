<Window x:Class="Monkeysoft.Screenshot.MonitorSelection"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Monkeysoft.Screenshot"
        mc:Ignorable="d" Title="MonitorSelection" WindowStyle="None" Width="800" Height="480"
        AllowsTransparency="True" Background="Transparent" Topmost="True" WindowStartupLocation="CenterScreen">
    <Border>
        <ListView Name="lstGallery" Margin="10" Padding="5" Background="Transparent"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding}" Width="240" Height="240"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Border>
</Window>
